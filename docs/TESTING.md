# 测试记录与真机验收

## 当前检查职责（自 0.1.4 起）

开发者运行 unittest、静态检查与安装包校验。仿真、Tk 冒烟、预览导出、视觉及真机验收由用户完成。保留仿真工具，不因未执行仿真而删除相关功能。下面历史版本的仿真记录不适用于新版；纯渲染单元断言仍属自动单元测试。

## 历史已执行的电脑检查

环境：Windows，Python 3.12，Pillow 11，requests 2.34；目标兼容 Python 3.9/Pillow 9，另通过 Python 3.9 语法检查。尚未用实际 Kindle Python 3.9 运行，库版本兼容需真机确认。

```text
python -m unittest discover -s tests -v
python tools/check.py
python tools/ui_smoke.py
python todoclock/run.py preview
```

自动测试覆盖：

0.1.3 结果：**74 项 unittest 通过**；静态检查与隐藏 Tk 冒烟测试通过。四个 KUAL shell 脚本通过 `sh -n` 检查；ZIP 的 CRC、逐文件 SHA256 清单及敏感文件排除检查通过。

- 原子写中断保留旧队列，坏 JSON 显式报错，配置合并与校验。
- 请求头认证、Host 校验、拒绝跳转、超时/429、错误脱敏。
- 天气三接口、部分失败、地点缓存隔离、概率转换、跨日预报选择。
- 微软刷新令牌、分页/循环/跨域拒绝、ETag 重试、离线去重、远端冲突与删除。
- 闰年、月份跨年、四方向坐标互逆、触摸完整帧与丢帧、仿射校准。
- 计时暂停/完成、切页完成提示、旋转确认回退、模态框禁止点击穿透。
- 恢复时 PID 身份匹配、重复恢复及失败保留记录。
- Tk 实际事件循环、图像展示、鼠标/按键分发、模拟工具栏和正常退出（窗口隐藏）。

生成五页 × 四方向共 20 张 PNG；检查横屏主要页面及竖屏天气、设置布局。检查不代表测得电子墨水残影、触摸精度或耗电。

网络测试使用 mock 数据；没有你的 Key/微软授权，因此**真实和风 API、Microsoft Graph 登录及写入未验证**。

## 0.1.1 回归检查

2026-09-05：新增 10 项确定性测试，覆盖无字体箭头、实体键方向、同批输入中的按下/松开、反白帧显示后才触发操作、拖出/取消/内容变更保护、DNS/TLS/超时脱敏、天气接口名与 HTTP 状态、封皮拔出覆盖残留节点、电量每秒检查及旧状态不覆盖新状态。上述测试、静态检查、隐藏 Tk 与 20 张页面渲染均通过。

用户报告旧版天气连接失败、箭头偶尔空白、无按下反馈、封皮/充电状态滞后。这是问题复现反馈；0.1.1 的电子墨水反白效果、实体键码、在位信号及真实天气连接仍待升级后回测。原日志只有 ServiceError，不能据此确定认证、网络或响应格式中的哪一项失败。

## 真机验收：新版修复待回测

| 项目 | 操作 | 合格标准 | 状态 |
|---|---|---|---|
| 依赖与显示 | 运行 diagnose / once | 1072×1448、8 bpp，图片显示正常 | 待验证 |
| 校准 | 四点校准、重启插件 | 触摸命中正确，误差校验通过 | 待验证 |
| 四个方向 | 逐次旋转、等待回退 | 图像与触摸一致，超时回到原方向 | 待验证 |
| 翻页键 | 待办切列表、日历切月 | 每按一次变化一次，方向符合握持 | 待验证 |
| 前光 | 0、增加、减少、退出 | 0 真正关闭，退出恢复原亮度 | 待验证 |
| Wi-Fi | 关闭、开启、退出 | 状态正确，已保存网络可重连，退出恢复 | 待验证 |
| 本体/封皮 | 插拔封皮、接通电源 | 两组电量和充电状态合理，无封皮隐藏 | 待验证 |
| 屏保 | 无触摸等待超过原屏保时间 | 信息屏继续显示并更新时间 | 待验证 |
| 原生覆盖 | 插件运行中等待/联网 | 不被主页或系统栏重绘遮挡 | 待验证 |
| 主动退出 | 设置/短按电源/双键长按 | 原生界面与输入恢复 | 待验证 |
| 异常恢复 | SSH 终止应用或暂停应用 | 看护恢复，原生界面可操作 | 待验证 |
| 和风 | 两地点分别测试连接 | 实时、每日、小时数据和错误提示正确 | 待验证 |
| 微软 | 登录、分页、完成测试任务 | 云端实际状态与屏幕一致 | 待验证 |
| 离线完成 | 断网勾选、重启、联网 | 队列保留并提交一次，失败不伪装成功 | 待验证 |
| 计时与刷新 | 前台秒级、后台完成 | 无明显漂移，残影可控，提示出现 | 待验证 |
| 持续运行 | 至少两小时混合使用 | 无输入丢失、过量请求、崩溃或异常增长 | 待验证 |

异常测试请先确认 SSH 和正常 stop 命令可用。读取 `/tmp/todoclock-runtime/owner.json` 中 child PID，在设备上核对 `/proc/PID/cmdline` 对应本插件后再发信号。不要凭旧 PID 或使用 killall 执行故障注入。`SIGSTOP` 模拟应用卡住，45 秒心跳阈值加终止等待和恢复命令时间后应恢复。

硬件失败时保留 `state/diagnostics.json`、脱敏日志和屏幕现象；不上传 token.json、secrets.json 或整个任务缓存。只有收集真机证据后才能将对应项改为通过。


## 0.1.2 回归检查

新增两项测试：官方地区子域名格式及大小写处理；多层域名中的空标签、非法连字符、超长标签、域名后缀伪装、用户信息、端口、路径和非 HTTPS 拒绝。共 63 项 unittest 通过；静态检查、隐藏 Tk 冒烟和 20 张页面渲染通过。未使用用户凭据发起请求或将凭据写入文件。
本轮首次并行执行时，已有计时测试在 Windows 原子替换临时文件时出现一次 PermissionError；未改代码，单独重跑完整 63 项测试通过。此现象记录为电脑环境的间歇文件访问失败，未推断为 Kindle 故障。


## 0.1.3 回归检查（2026-09-06）

新增 11 项测试，并更新旧版“等待闪完”和“仅凭 present”相关断言。74 项 unittest 通过。覆盖慢速显示时连续点击不丢操作、只保留最新待显示帧、全刷标志保留、显示错误脱敏、退出等待活动写入并丢弃待显示帧、页面变更取消手势、任务稳定 ID、天气实体键与空数据边界、字体无关符号和图标、封皮装卸样例、USB 信号冲突、供电与充电分离、权限及局部读取失败、别名去重。

用户提供的拔出/装上样例已作为合成 sysfs fixture：soda_fg 无 present，装上时 Full/100，拔下时仅 type；同时存在 charger present=0、online=1。此测试验证判定算法，不说明未知的原始读取失败必然属于 missing/no_data。新诊断补充读取分类，其他 I/O 故障保持未知。

静态检查、隐藏 Tk 冒烟、五页四方向 20 张 PNG 通过。另执行 python tools/visual_checks.py，导出反白、横竖屏长天气文字/未知代码/缺失概率和 13 种图标图集；人工检查天气横竖布局、长文本截断、计时加减符号。未新增真实服务请求，未连接 Kindle。

真机仍待验收：快速点击手感、实际封皮插拔与 USB 供电状态、屏幕残影和远距离可读性、异常恢复以及持续运行。此前用户反馈是旧版故障证据，不标记为新版通过。


## 0.1.4 检查记录

新增 6 项单元测试，覆盖同步完整性、后台任务/部分失败/待提交队列、过期与仅手动同步、合并本体充电提示且不修改封皮数据、设置页码/实体键边界、全刷动作不修改周期和页面。共 80 项 unittest 通过；静态检查和安装包校验通过。

按用户要求，本版未执行仿真、Tk 冒烟、预览导出或视觉验收。待用户验收：顶部三列信息排版、放大天气及图标、同步勾号/叹号、设置页码与实体键、左下全刷按钮。preview 目录里的图片来自旧版本。
